{
  "name": "backend",
  "version": "1.0.0",
  "description": "top social ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch db.json --port 3000",
    "server": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.21"
  }
}
